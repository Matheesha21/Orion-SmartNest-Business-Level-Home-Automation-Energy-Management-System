# Basic ACL allowing authenticated users full access; tighten per-topic in production
user homeassistant
topic readwrite #

# Example: restrict a user to specific topic
# user guest
# topic read sensors/+
